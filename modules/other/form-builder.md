# Form Builder

Form Builder’s admin tables follow the same [admin form chrome](../admin/forms.md#tables) as the rest of admin: no `.table-bordered`, flush inside fieldsets, and “+ Add Item” in `<tfoot>` for dynamic rows.
